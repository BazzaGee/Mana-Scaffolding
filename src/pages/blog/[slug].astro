---
import Base from '../../layouts/Base.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const blogPosts = await getCollection('blog');
  return blogPosts.map((post) => ({
    params: { slug: post.id.replace(/\.md$/, '') },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await render(post);
---

<Base title={post.data.title} description={post.data.description}>
  <main id="main-content" class="pt-24 pb-16">
    <article class="py-16 bg-background">
      <div class="container mx-auto px-6 max-w-4xl">
        <header class="mb-12 reveal-on-scroll">
          <div class="flex items-center gap-3 mb-4">
            <span class="text-label-xs font-bold text-primary uppercase tracking-widest">{post.data.category}</span>
            <span class="text-zinc-600">•</span>
            <span class="text-label-xs text-zinc-500">{post.data.pubDate.toLocaleDateString('en-NZ', { year: 'numeric', month: 'long', day: 'numeric' })}</span>
          </div>
          <h1 class="text-4xl md:text-6xl font-headline font-black uppercase tracking-tighter text-white mb-6">
            {post.data.title}
          </h1>
          <p class="text-xl text-zinc-400 leading-relaxed">
            {post.data.description}
          </p>
          <div class="flex items-center gap-4 mt-6 pt-6 border-t border-white/5">
            <span class="text-zinc-500">By</span>
            <span class="text-white font-bold">{post.data.author}</span>
          </div>
        </header>
        
        <div class="prose prose-invert prose-lg max-w-none reveal-on-scroll">
          <Content />
        </div>
        
        <footer class="mt-16 pt-8 border-t border-white/5">
          <div class="flex flex-wrap gap-2 mb-8">
            {post.data.tags.map((tag) => (
              <span class="text-label-xs uppercase tracking-widest bg-white/5 px-3 py-2 text-zinc-400">#{tag}</span>
            ))}
          </div>
          <a href="/blog" class="text-primary hover:text-orange-600 transition-colors font-headline font-bold uppercase tracking-widest">
            ← Back to all articles
          </a>
        </footer>
      </div>
    </article>
  </main>
</Base>